import React from "react";
import ReactDOM from "react-dom/client";
import { Canvas } from "./index";
ReactDOM.createRoot(document.getElementById("go-bite") as HTMLElement).render(
  <React.StrictMode>
    <Canvas />
  </React.StrictMode>
);
